# タンパク質の機能予測、SFTからRLでどれだけ賢くなったか

## この記事について

タンパク質のアミノ酸配列から、その機能(Gene Ontology、GO term)を予測するタスクに、「まず考えてから答える」タイプの言語モデルを適用する実験をしてきました。教師ありファインチューニング(SFT)のあと、強化学習(RL)でさらに鍛えるとどれくらい賢くなるのか、これまで何度か検証してきましたが、なかなかはっきりした差が出ませんでした。今回、学習の規模とやり方を見直したところ、これまでで最もはっきりとRLの効果が確認できましたので、その内容をまとめます。

## どんな問題を解いているか

扱っているタスクは、あるタンパク質のアミノ酸配列とその周辺情報(ドメイン構成や相互作用相手など)を入力として与え、そのタンパク質が持つ生物学的な機能をGO term(分子機能・生物学的プロセス・細胞内局在の3つの側面)として予測するというものです。ただの分類ではなく、モデルにはまず`<think>`ブロックの中で根拠を整理しながら考えさせ、そのうえで最後にGO termのリストを答えさせる、という構成にしています。これにより、予測の根拠を人間があとから読んで検証できるようにしています。

ベースにしているのはBioReason-Proという論文の設定で、タンパク質配列をエンコードする専用モデル(ESM)と、テキストで推論する言語モデル(Qwen3-4B-Thinking)を組み合わせたアーキテクチャです。今回の実験では、論文とほぼ同じ設定を再現していますが、唯一、タンパク質のエンコーダーだけは論文が使っているESM3ではなくESM2を使っています。これはESM3のライセンス上の制約によるもので、それ以外(言語モデル本体、学習方法、報酬の設計)は論文の設計にできる限り合わせています。またモデルへの入力には、配列そのものに加えて、ドメイン注釈(InterPro)、相互作用パートナー(STRING)、細胞内局在、そして別の専用モデル(GO-GPT)による予測GO termも与えています。GO-GPTの予測をヒントとして与える点は、これまで抜けていた論文との差分でしたが、今回の実験からきちんと反映しています。

学習はまずSFTで「配列を読んで、考えて、GO termを答える」という型を教え込み、そのあとRL(GRPOという手法)でさらに磨き込むという二段階構成です。RLの報酬は、予測したGO termが正解とどれだけ一致しているか(weighted F_max)を基本に、考えている内容が実際に予測の根拠として使われているか(根拠に忠実か)を測る小さなボーナスを加えたものにしています。

## 使ったデータ

学習には、CAFA5という国際コンペティションのデータをもとに作った、実際のタンパク質配列と、その機能を導く思考過程(reasoning)付きの正解データセットを使っています。SFTではこの思考過程ごと模範解答として学習し、RLでは同じ配列プールに対してモデル自身に何度も予測を生成させ、その正解度合いを報酬として学習します。

そして最終的な評価には、`bioreason_pro_test`という8,630件のタンパク質からなる、封印されたテストセットを使いました。これは学習データが作られた時点ではまだ機能注釈がついていなかったタンパク質だけを集めた「事前知識なし」のセットで、開発中は一切参照せず、今回の最終確認のためだけに一度だけ開封して使っています。

## 学習の様子

SFTの学習run([phase3-sft-1640](https://wandb.ai/wandb-healthcare/bioreasonpro-senpai/runs/ktq03cwy))では、学習が進むにつれ損失(loss)が全体としては下がっていく傾向が見られました。推論トレースもほぼ全件(99.2%)できちんと出力されるようになっており、SFTの段階でも「考えてから答える」型は十分に身についています。学習中にモニタリング用として簡易的に見ている内部指標(訓練データの一部を使った256件のサブセットでのweighted F_max)も0.729まで到達しています。

そのあとのRLの学習run([phase4-grpo-1771](https://wandb.ai/wandb-healthcare/bioreasonpro-senpai/runs/85vrztci))では、100ステップかけて報酬(予測GO termの正解率に基づく値)を最大化していきました。報酬の推移自体は単調に右肩上がりというよりはばらつきが大きいものでしたが、最初の10ステップの平均が0.56だったのに対し、最後の10ステップの平均は0.77まで上がっており、全体としては着実に伸びていることが確認できます。同じ内部モニタリング指標も、SFT時点の0.729から0.859まで上昇しました。ただしこれらはあくまで学習中の簡易チェック用の数字で、本当の実力を測るのは次の節で紹介する、学習中は一切触れていない封印テストセットでの結果です。

## 結果:SFT vs RL

最終的な判断材料として一番重視しているのは、学習中には一切見ていない封印テストセット(8,630件)での結果です。

| | SFT | RL | 差分 |
|---|---:|---:|---:|
| weighted F_max(全体) | 0.480 | 0.574 | **+0.093** |
| 分子機能(MF) | 0.513 | 0.585 | +0.072 |
| 生物学的プロセス(BP) | 0.347 | 0.472 | +0.125 |
| 細胞内局在(CC) | 0.581 | 0.664 | +0.083 |

3つの側面すべてでRLがSFTを上回っており、特に生物学的プロセス(BP)の伸びが大きくなっています。

この結果を論文自身の数字と比べてみると、違いがより際立ちます。論文自身の報告では、SFTのweighted F_maxが0.64、RLが0.66と、その差はわずか0.02にとどまっており、論文中でも「RLはSFTをかろうじて上回る程度」と評されています。さらに論文の人手評価(専門家によるブラインド評価)では、むしろSFTの方がRLより高く評価されるという結果も出ており、RLの効果自体があまり安定していませんでした。それに対して今回の結果は、+0.093というより大きな、かつ3側面すべてで一貫した改善になっており、論文よりもはっきりとRLの効果が出た形です。この違いは、1バッチあたりに使うタンパク質数やロールアウト数、学習ステップ数を大きくし、論文と同じ「バッチ全体でまとめて分散を正規化する」方式を採用したことが効いていると考えています。

## RLで具体的に何が良くなったのか

数字の上での改善だけでなく、実際にモデルが書いた推論を読み比べてみると、RLによって何が変わったのかがよりはっきり見えてきます。学習run自体の指標を見ても、答えが根拠に基づいているかを測る指標(reasoning faithfulness)がSFTの0.899からRLの0.942に、答えが指定した長さに収まらず尻切れになってしまう割合(answer truncated)がSFTの9.4%からRLの3.1%へと改善しており、「より根拠に基づいた」「より最後まで書ききれる」答えになる方向に変化していることが数字からもうかがえます。

ここでは、封印テストセットの中から、SFTでは不正解だったのにRLでは正解できていた例を2つ取り上げ、実際に何が起きているのかを見てみます。両方とも面白いことに、SFTとRLは生物学的にはほぼ同じ、正しい結論にたどり着いています。違いは、その結論を「正式なGO IDとして明示的に書き出しているかどうか」という一点にありました。

### 例1:核内のタンパク質メチル化酵素

あるタンパク質(KMT2C様のドメイン構成を持つもの)について、SFTとRLはどちらもInterProのドメイン構成(ヒストンのマークを読み取るPHDドメイン、メチル基を転移するSETドメインなど)を丁寧に読み解き、「このタンパク質は核内、具体的には核質(nucleoplasm)で働くヒストンメチル化酵素である」という、ほぼ同じ結論に達しています。実際、SFTの回答にも「the protein in the nucleus, specifically within the nucleoplasm」という一文があり、生物学的な理解としては申し分ありません。

しかし、正解判定は「回答の中に`GO:0005654`(nucleoplasmのGO ID)のような正式なコードが書かれているか」で機械的に行われます。SFTは最後まで「nucleoplasm」という言葉だけで説明を終えてしまい、正式なコードを一度も書きませんでした。一方RLは、同じ結論に達したうえで「the GO cellular component label GO:0005654 nucleoplasm aligns with...」と、正式なGO IDを本文中に明記していました。GO termは親子関係を持つ階層構造になっているため、`GO:0005654`(核質)を正しく示せば、その上位にあたる「核」「細胞内小器官」「細胞内構造」といった関連する正解ラベルも芋づる式に正解扱いになります。その結果、正解13個中のスコアが、SFTの0点に対しRLはほぼ満点という、大きな差になりました。

### 例2:細胞接着に関わるタンパク質(talin)

もう一つの例は、細胞外基質と細胞骨格をつなぐ足場タンパク質talinです。SFTとRLはどちらも、FERMドメインやI/LWEQドメインといった構造を非常に詳しく解析し、「このタンパク質はインテグリンのβ鎖に高親和性で結合する」という、ほぼ同一の結論に達しています。SFTの回答にも「high-affinity integrin β-tail binding」という表現がはっきり出てきます。

ここでも差がついたのは同じ理由でした。RLは結論部分で「integrin binding (GO:0005178)」と正式なコードを添えて書いたのに対し、SFTは英語の説明だけで押し通し、`GO:0005178`という文字列を一度も書きませんでした。生物学的な理解の深さそのものは互角と言ってよく、違いはあくまで「答えを正式な語彙(GO ID)に変換して書き出す」という一手間にありました。

この2つの例が示しているのは、RLによる改善が単に「より賢い推論ができるようになった」という話ではなく、**「わかっていることを、採点可能な正式な形式で明示的に書き出す」という書き方の癖が身についた**、という点です。RLの報酬は回答中の「GO:数字7桁」というパターンを機械的に拾って正解率を計算する仕組みになっているため、同じ内容を答えるにしても、正式なIDを本文に埋め込んだ方が高い報酬を得られます。RLはこの一貫した練習を通じて、正しい生物学的理解を、より明示的で、後から機械的にも人間にも検証しやすい形で書き出すようになった、と言えそうです。これは学習run全体の指標(根拠に基づいた回答をしている度合いが0.899→0.942に上昇)とも整合する変化です。

## まとめ

今回のスケールアップと設計変更(GO-GPTの予測をヒントとして追加、1バッチあたりのタンパク質数とロールアウト数を増やし論文と同じ分散正規化方式を採用、100ステップの学習)により、封印テストセットでweighted F_maxが0.480から0.574へと、3つのGOの側面すべてで一貫して向上する結果が得られました。この+0.093という改善幅は、論文自身が報告するRLの改善幅(+0.02、しかも人手評価では逆転)よりもはっきりとした、安定した効果です。実際の回答を読み比べると、RLはSFTと同程度に正しい生物学的推論をしながらも、その結論を正式なGO IDとして明示的に書き出す習慣を身につけており、これが採点上のスコア向上に直結していることも確認できました。まだ1回の学習(1 seed)による結果であるため、複数回の学習を重ねた統計的な検証は今後の課題として残っていますが、これまでの検証の中で最も明確に「RLがSFTを上回る」ことを示せた結果だと考えています。

---

*参考:今回の封印テストセットでの全8,630件の予測結果は、Weaveの [SFT側](https://wandb.ai/wandb-healthcare/bioreasonpro-senpai/weave/evaluations?name=phase5-holdout-sft-phase5-holdout-1846) / [RL側](https://wandb.ai/wandb-healthcare/bioreasonpro-senpai/weave/evaluations?name=phase5-holdout-sft_to_grpo-phase5-holdout-1844) のEvaluationとして公開されており、タンパク質ごとの推論と正解ラベルを直接見比べることができます。*
